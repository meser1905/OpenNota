/**
 * @opennota/db
 *
 * Re-exports the generated Prisma client and all model types so the rest of
 * the monorepo depends on a single stable package name rather than reaching
 * into `@prisma/client` directly.
 */
export * from '@prisma/client';
